import runpy
import xlwt
from xlwt import Workbook
import gun_prediction
import sys
import importlib

#counts = dict()
# print(gun_prediction.gun_name)
#for x in range(10):
 #   if tuple(gun_prediction.gun_name) in counts:
  #      counts[tuple(gun_prediction.gun_name)] += 1
   # else:
    #    counts[tuple(gun_prediction.gun_name)] = 1
    #importlib.reload(gun_prediction)
    #import gun_prediction
    #runpy.run_module(mod_name='gun_prediction')
    #sys.modules['gun_prediction'].__dict__.clear()
    #importlib.reload(gun_prediction)
    #exec(open('gun_prediction.py').read())
#print(counts)


# for word in words:
#         if word in counts:
#             counts[word] += 1
#         else:
#             counts[word] = 1
